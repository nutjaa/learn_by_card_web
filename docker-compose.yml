version: '3'

services:
  nginx:
    image: nginx:alpine
    ports:
      - '3004:80'
    volumes:
      - ./build:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
